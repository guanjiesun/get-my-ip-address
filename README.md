### 使用不同的 API 获取 IP 地址
    1. IPCN
    2. IPIN
    3. WHATISMYIP
    4. HTTPBIN

### 三个版本
    1. Python 多线程
    2. Python 多协程(异步)
    3. JavaScript 多Promise(异步)

### 注意
第一次运行 `node js_version.js` 可能会网络超时, 请多运行几次
